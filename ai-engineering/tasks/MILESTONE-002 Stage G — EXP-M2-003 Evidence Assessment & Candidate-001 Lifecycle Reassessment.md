# MILESTONE-002 Stage G — EXP-M2-003 Evidence Assessment & Candidate-001 Lifecycle Reassessment

## Execution Mode

You are executing:

```text
MILESTONE-002 Stage G
EXP-M2-003 Evidence Assessment &
CANDIDATE-001 Lifecycle Reassessment
```

Repository:

```text
evan-ai-auto/context-engine
```

This is an **assessment-only stage**.

Do not start a new engineering experiment unless explicitly required to resolve an evidence contradiction.

Do not package CANDIDATE-001 or CANDIDATE-002.

Do not create SKILL.md, WORKFLOW.md, Agent runtime definitions, orchestration code, or automatic invocation mechanisms.

---

# 1. Objective

Assess the evidence generated by:

```text
EXP-M2-001
EXP-M2-002
EXP-M2-003
```

with particular focus on the new dependency-composition evidence from EXP-M2-003:

```text
CANDIDATE-001
    ↓
Validation Requirement Determination
    ↓
Validation Request
    ↓
CANDIDATE-002
    ↓
Validation Evidence
    ↓
CANDIDATE-001 Evidence Consumption
    ↓
Revision Disposition
```

The objective is to determine whether the Stage E disposition of CANDIDATE-001 should change.

Current authoritative lifecycle state:

```text
CANDIDATE-001 = CONDITIONALLY_VALIDATED
```

Current Stage E disposition:

```text
PROMOTE_WITH_CONDITIONS
```

Stage F outcome:

```text
EXP-M2-003 = SUCCESS
Dependency Gap Closure = PARTIALLY_CLOSED
Failure Recovery = NOT TESTED
Packaged Skill Invocation = NOT TESTED
```

---

# 2. Critical Governance Rule

Do NOT assume:

```text
EXP-M2-003 SUCCESS
        ↓
CANDIDATE-001 VALIDATED
```

That conclusion must be earned from evidence.

The assessment must explicitly distinguish:

```text
Experiment Outcome
        ≠
Evidence Sufficiency
        ≠
Asset Validation
        ≠
Implementation Readiness
        ≠
Packaging Authorization
```

Also distinguish:

```text
Happy-Path Dependency Composition
        ≠
Failure-Path Composition
```

and:

```text
Design-Document Procedure Invocation
        ≠
Packaged Skill Runtime Invocation
```

---

# 3. Mandatory Pre-Read

Before assessment, inspect:

```text
ai-engineering/milestones/MILESTONE-002/MILESTONE-002.md

ai-engineering/milestones/MILESTONE-002/
01-validation-experiment-framework.md
02-stage-b1-first-experiment-selection.md
03-stage-b2-exp-m2-001-experimental-invocation.md
04-stage-b3-exp-m2-001-evidence-and-assessment.md
05-stage-c1-evidence-gap-and-second-experiment-selection.md
06-stage-c2-exp-m2-002-experimental-invocation.md
07-stage-c3-exp-m2-002-evidence-and-assessment.md
08-stage-d-cross-experiment-evidence-synthesis.md
09-stage-e-stage-evidence-sufficiency-and-asset-disposition.md
10-stage-f-exp-m2-003-invocation-and-evidence-capture.md

Candidate definitions:
CANDIDATE-001
CANDIDATE-002
CANDIDATE-003
CANDIDATE-004
```

Also inspect relevant git history and diffs where necessary.

Do not rely only on MILESTONE-002.md summary.

---

# 4. Assessment Questions

The assessment MUST answer all of the following.

## Q1 — Did EXP-M2-003 close the Stage E critical dependency gap?

Evaluate:

```text
DEPENDENCY_IDENTIFIED
DEPENDENCY_REQUESTED
DEPENDENCY_INVOKED
DEPENDENCY_SUCCEEDED
EVIDENCE_PRODUCED
EVIDENCE_CONSUMED_BY_001
```

Determine independently whether each state is:

```text
OBSERVED
SUPPORTED_INFERENCE
WEAK_INFERENCE
NOT_ESTABLISHED
```

Do not simply copy the Stage F classification.

---

# 5. Q2 — How strong is the dependency evidence?

Assess:

### Request evidence

Was there a real validation request?

```text
VR-M2-003-001
```

Determine whether this is:

```text
DIRECTLY_OBSERVED
```

or merely documented after the fact.

### Invocation evidence

Determine whether CANDIDATE-002 was actually invoked according to its procedure.

Important distinction:

```text
procedure execution
vs
mere statement that execution occurred
```

Look for actual gate execution evidence.

### Result evidence

Verify the claimed:

```text
pytest
ruff
mypy
```

results.

Do not treat these commands as proof of CANDIDATE-002 by themselves.

Determine whether the evidence supports:

```text
CANDIDATE-002 procedure executed
```

### Consumption evidence

Determine whether CANDIDATE-001 actually used the aggregate evidence to make its revision disposition.

The required chain is:

```text
Evidence Produced
        ↓
Evidence Received
        ↓
Evidence Interpreted
        ↓
Engineering Decision Changed / Confirmed
```

---

# 6. Q3 — Does Stage F demonstrate composability?

Evaluate whether the following behavior has now been experimentally demonstrated:

```text
CANDIDATE-001
can identify a validation requirement
and request another asset
and consume the resulting evidence.
```

Classify each separately:

```text
Validation Requirement Determination
Validation Request
Dependency Invocation
Evidence Reception
Evidence Interpretation
Evidence Consumption
Revision Disposition
```

Use:

```text
OBSERVED
SUPPORTED_INFERENCE
WEAK_INFERENCE
NOT_ESTABLISHED
```

Do NOT collapse the entire chain into one binary result.

---

# 7. Q4 — What remains unvalidated?

Explicitly identify remaining gaps.

At minimum evaluate:

```text
1. Failure / ERROR path
2. Dependency failure handling
3. Packaged Skill invocation
4. Independent replication
5. Different repository/task context
6. Multi-asset composition beyond 001 → 002
7. Human intervention / judgment dependence
```

Do not automatically classify all seven as blockers.

For each gap determine:

```text
Required for current lifecycle?
Required before packaging?
Useful future evidence?
Not necessary?
```

---

# 8. Evidence Matrix

Create an updated evidence matrix for CANDIDATE-001.

Required dimensions:

| Dimension | Assessment |
|---|---|
| Evidence Breadth | |
| Behavioral Repeatability | |
| Task Diversity | |
| Attribution Strength | |
| Dependency Coverage | |
| Candidate Validation | |
| Failure Coverage | |
| Human Intervention | |
| Reproducibility | |
| Evidence Quality | |

Use controlled vocabulary where appropriate:

```text
LOW
MODERATE
HIGH

NOT_ESTABLISHED
LIMITED
PARTIAL
SUFFICIENT
```

Do not introduce stronger terminology without justification.

---

# 9. Cross-Experiment Assessment

Compare:

```text
EXP-M2-001
EXP-M2-002
EXP-M2-003
```

Create a table:

| Behavior | M2-001 | M2-002 | M2-003 | Overall |
|---|---|---|---|---|
| Task inspection | | | | |
| Revision boundary definition | | | | |
| Engineering execution | | | | |
| Validation requirement determination | | | | |
| Validation delegation | | | | |
| Evidence consumption | | | | |
| Revision disposition | | | | |
| Boundary preservation | | | | |

Classify overall behavior as:

```text
REPEATED
PARTIALLY_REPEATED
SINGLE-EXPERIMENT
CONTEXT-DEPENDENT
NOT_ESTABLISHED
```

---

# 10. Lifecycle Decision

This is the central decision of Stage G.

Evaluate whether CANDIDATE-001 should remain:

```text
CONDITIONALLY_VALIDATED
```

or move to:

```text
VALIDATED
```

or another already-established lifecycle state.

Do NOT invent a new lifecycle state merely to avoid a decision.

The decision must be based on:

```text
Evidence sufficiency
Behavioral repeatability
Dependency evidence
Failure coverage
Attribution
Reproducibility
Known limitations
```

---

# 11. Decision Standard

Use this logic:

### VALIDATED may only be considered if:

```text
Core behavior is sufficiently demonstrated
AND
evidence is attributable
AND
behavior is repeatable enough for the intended scope
AND
known critical dependency behavior is sufficiently demonstrated
AND
remaining gaps do not undermine the intended lifecycle claim
```

Otherwise retain:

```text
CONDITIONALLY_VALIDATED
```

Do not interpret:

```text
Happy-path dependency success
```

as sufficient by itself for unconditional validation.

---

# 12. Packaging Readiness

Independently assess:

```text
Is CANDIDATE-001 ready to be packaged as SKILL.md?
```

Possible result:

```text
READY
NOT_READY
READY_WITH_EXPLICIT_CONDITIONS
```

Do not package it during Stage G.

The assessment must explain the result.

Also assess CANDIDATE-002 independently.

Do not assume that validating the interaction:

```text
001 → 002
```

validates 002 as an independent reusable asset.

---

# 13. Conditions for Promotion

If lifecycle remains:

```text
CONDITIONALLY_VALIDATED
```

define explicit conditions.

Examples may include:

```text
- failure-path composition test
- packaged procedure validation
- independent replication
- broader task diversity
```

But include only conditions justified by the evidence.

Do not create an unnecessarily large validation backlog.

Each condition must state:

```text
Condition
Reason
Evidence needed
Why it matters
```

---

# 14. Historical Integrity

Do NOT rewrite historical experiment records.

Do NOT change the factual outcome of:

```text
EXP-M2-001
EXP-M2-002
EXP-M2-003
```

Do NOT rewrite Stage E conclusions as if Stage F had already existed.

Stage G may interpret previous evidence, but historical records remain immutable in substance.

---

# 15. No New Experiment

Stage G is assessment-only.

Do NOT:

```text
run another experiment
modify production behavior
modify Candidate definitions
package assets
create orchestration code
create runtime Skill files
```

If a contradiction is discovered, record it explicitly.

Do not silently resolve it by modifying historical evidence.

---

# 16. Required Stage G Record

Create:

```text
ai-engineering/milestones/MILESTONE-002/11-stage-g-exp-m2-003-evidence-assessment-and-lifecycle-reassessment.md
```

The document MUST contain:

```text
1. Objective
2. Assessment Scope
3. Authoritative Evidence
4. EXP-M2-003 Dependency Chain Assessment
5. Evidence Attribution Assessment
6. Evidence Consumption Assessment
7. Cross-Experiment Comparison
8. Updated Evidence Matrix
9. Remaining Evidence Gaps
10. CANDIDATE-001 Lifecycle Assessment
11. CANDIDATE-001 Packaging Readiness
12. CANDIDATE-002 Independent Assessment
13. Conditions / Follow-Up Requirements
14. Decision
15. Historical Integrity Statement
16. Conclusion
```

---

# 17. Decision Record

The final decision section MUST explicitly state:

```text
CANDIDATE-001 Lifecycle:
<state>

Stage E Disposition:
<state>

Stage F Impact:
<summary>

Dependency Composition:
<state>

Failure Path:
<state>

Packaging Readiness:
<state>

Next Recommended Step:
<one clearly prioritized step>
```

The final decision must not be ambiguous.

---

# 18. MILESTONE-002 Update

Update:

```text
ai-engineering/milestones/MILESTONE-002/MILESTONE-002.md
```

Only with Stage G completion/status information.

Do not rewrite prior stage history.

If Stage G determines that lifecycle remains:

```text
CONDITIONALLY_VALIDATED
```

preserve that state explicitly.

If Stage G determines a lifecycle change is justified, record the evidence and rationale precisely.

---

# 19. Validation of the Assessment Artifact

Although Stage G is assessment-only, perform:

```bash
git diff --check
```

If repository checks are appropriate, also run:

```bash
pytest
ruff check .
mypy src
```

These are artifact/repository hygiene checks.

They are NOT new asset-validation evidence.

Do not claim Stage G has experimentally validated CANDIDATE-001 merely because these commands pass.

---

# 20. Git Review

Before commit:

```bash
git status
git diff --stat
git diff
```

Confirm:

```text
No historical experiment record rewritten
No production code changed
No candidate definition redesigned
No packaged Skill created
No unrelated files changed
```

---

# 21. Commit

Use a clear commit message such as:

```text
docs(milestone-002): reassess candidate-001 lifecycle after exp-m2-003
```

Push the commit.

---

# 22. Final Execution Report

After push, report:

```text
MILESTONE-002 Stage G completed.

Assessment:
<result>

EXP-M2-003 Dependency Composition:
<result>

CANDIDATE-001 Lifecycle:
<state>

Stage E Disposition:
<state>

Packaging Readiness:
<result>

Remaining Critical Gaps:
<list>

Files Changed:
<list>

Checks:
pytest: PASS/FAIL/NOT RUN
ruff: PASS/FAIL/NOT RUN
mypy: PASS/FAIL/NOT RUN
git diff --check: PASS/FAIL

Commit:
<sha>

Push:
SUCCESS/FAILED
```

---

# 23. STOP CONDITION

After successful push:

```text
STOP.
```

Do NOT automatically proceed to:

```text
Skill Packaging
Workflow Packaging
Agent Packaging
Failure Experiment
CANDIDATE-003 validation
CANDIDATE-004 validation
```

Stage G only determines the evidence-based lifecycle position and the single most valuable next step.

End execution after reporting the result.